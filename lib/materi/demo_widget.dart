import 'package:flutter/material.dart';

class MateriDemo extends StatelessWidget {
  const MateriDemo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Materi Page'),
      ),
    );
  }
}
